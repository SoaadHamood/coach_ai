# coach-ai

AI-assisted call coaching prototype with a Streamlit UI.
Records audio, transcribes it, and provides coaching insights + post-call summary.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
